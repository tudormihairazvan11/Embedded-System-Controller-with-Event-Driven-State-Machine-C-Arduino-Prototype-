# Embedded-System-Controller-with-Event-Driven-State-Machine-C-Arduino-Prototype-
designed and implemented an embedded system controller using an event-driven state machine architecture. The firmware processes analog sensor input, handles user interaction via button events, and controls visual and acoustic outputs using non-blocking timing logic. 
